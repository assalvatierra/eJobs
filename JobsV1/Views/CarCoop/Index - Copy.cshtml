@model IEnumerable<JobsV1.Models.CarUnit>

@{
    //ViewBag.Title = "Car Rental Davao";
    ViewBag.data = 1;
}


<div class="jumbotron row" style="background-image:url('/Images/POTTMPC/POTTMPCBanner2.png');background-position:center;background-repeat:no-repeat;background-size:100%;height:420px;min-width:400px;">
    <div class="col-md-5">
        <h2>Welcome to Palawan!</h2>
        <p class="lead">Start your journey with us.</p>
        @Html.ActionLink("Contact us", "Contact", "CarCoop", null, new { @class = "btn btn-primary btn-lg" })
        <!--
        <button href="" class="btn btn-primary" id="catbtn" data-toggle="modal" data-target="#ReserveCarList" style="margin-bottom:10px; font-size:18px; margin:1px;"> Reservation </button>
        -->

        @Html.ActionLink("Reservation", "FormRenter", "CarCoop", new { id = 1 }, new { @class = "btn btn-primary btn-lg" })
    </div>

    <div class="col-md-5">

    </div>

    <div class="col-md-2">

    </div>
    @*@Html.ActionLink("Book a Car", "Create", "JobMains", null, new { @class = "btn btn-primary btn-lg" })*@

</div>


<div class="row">
    @foreach (var item in Model)
    {

        <div class="col-md-4 car-primary">
            <h2>@Html.DisplayFor(modelItem => item.Description)</h2>
            <div style="background-image:url('/CarRental/MainImage/@Html.DisplayFor(modelItem => item.Id)');background-position:center;background-repeat:no-repeat;background-size:200px;height:180px;">
                <p>&nbsp</p>
            </div>

            <div>
                <table>
                    <tr>
                        <th>
                            @Html.DisplayFor(modelItem => item.Remarks)  &nbsp;
                        </th>
                    </tr>
                    <tr>
                        <td>
                            @Html.Action("CarRate", new { unitid = int.Parse(@Html.DisplayFor(modelItem => item.Id).ToString()) })
                        </td>
                    </tr>
                </table>
            </div>
        </div>

    }
</div>

<div class="row">
    <div class="col-md-12">
        @{Html.RenderPartial("Partial_content_1");}
    </div>
</div>


@Html.Action("MobileModalView")